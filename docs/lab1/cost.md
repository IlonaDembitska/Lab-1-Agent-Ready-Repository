# Облік токенів і вартості

## Реалізація

Додано файл `src/cost.ts`, який:

- приблизно оцінює кількість токенів у тексті;
- оцінює вартість запиту до моделі;
- рахує вартість за фактичним usage;
- використовує ціни моделей із `src/models.ts`.

## Тести

Додано `tests/cost.test.ts`.

Результат:

- 5 test files passed;
- 64 tests passed;
- `npm run typecheck` — без помилок.

## Оцінка до виклику та фактичний usage
Перевірка на Google Gemini `gemini-3.1-flash-lite`:

- оцінка вхідних токенів до виклику: 14135;
- фактична кількість вхідних токенів: 14135;
- похибка: 0.0%;
- результат: у межах допустимих 10%.

Перший запуск:
- input: 14135;
- cached: 0;
- output: 62;
- вартість за `models.ts`: $0.003627;
- затримка: 3994 мс.

Другий запуск:
- input: 14135;
- cached: 8171;
- output: 66;
- вартість за `models.ts`: $0.003633;
- затримка: 3966 мс.
TODO: додати результати вимірювання.

## Українська та англійська

TODO: порівняти кількість токенів для однакового змісту українською та англійською.
## Українська та англійська

```ua
Перевір стан сервісу та поверни коротку відповідь.
```

```en
Check the service status and return a short response.
```
Результат для Ollama `qwen3:4b`:

- українська: 23 токени;
- англійська: 9 токенів;
- множник `ua/en`: 2.56.

Для цього прикладу український текст потребує приблизно у 2.56 раза більше токенів, ніж англійський.

## Кешування


TODO: перевірити вплив кешованого префікса.
Перевірка кешу на Ollama `qwen3:4b`:

- перший запуск: 2012 вхідних токенів, 0 кешованих, 64 вихідних;
- другий запуск: 2012 вхідних токенів, 2011 кешованих, 64 вихідних;
- затримка зменшилась приблизно з 65365 мс до 7390 мс.

Повторний виклик використав кешований префікс.

## Висновок

Реальна оцінка токенів для Gemini збіглася з фактичним usage з похибкою 0.0%. Повторні виклики показали роботу кешу як у Gemini, так і в Ollama. Для прикладу з однаковим змістом український текст у Ollama `qwen3:4b` використав у 2.56 раза більше токенів, ніж англійський.